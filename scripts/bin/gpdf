#!/usr/bin/env bash

echo

message="$@"
dotfiles_dir="$HOME/Dev/dotfiles"

if [[ ! "${mesage}" ]]; then
  message="update dotfiles"
fi

# go to dotfiles folder
cd "${dotfiles_dir}" 
git add --all
git commit -m "${message}"
git push
