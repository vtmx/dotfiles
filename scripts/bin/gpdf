#!/usr/bin/env bash

echo

message="$@"
dotfiles_dir="$HOME/Dev/dotfiles"

if [[ ! "${mesage}" ]]; then
  message="update dotfiles"
fi

# Go to dotfiles dir
cd "${dotfiles_dir}" 

# Git commands
git add --all
git commit -m "${message}"
git push
